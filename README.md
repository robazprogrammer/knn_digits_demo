# knn_digits_demo
A simple Python demonstration of K-Nearest Neighbors (KNN) using scikit-learn's handwritten digits dataset, including scaling, k comparison, model evaluation, and nearest-neighbor inspection.
